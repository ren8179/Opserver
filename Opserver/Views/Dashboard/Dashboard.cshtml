@using StackExchange.Opserver.Controllers
@using StackExchange.Opserver.Views.Dashboard
@model DashboardModel
@{
    this.SetPageTitle("仪表板");

    this.SetTopSearch("所有节点",
              Model.Filter,
              Url.Action(nameof(DashboardController.Dashboard)));
}
@section head {
    <script>
    $(function() {
        Status.Dashboard.init({ refresh: 60, filter: '@(Model.Filter)' });
    });
    </script>}
<div>
    @Html.Partial("Dashboard.Table", Model)
</div>